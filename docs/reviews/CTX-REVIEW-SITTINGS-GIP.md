# Independent review — CTX-REVIEW-SITTINGS-GIP

**This record owns** the independent review of the widen sittings 1–2 slice and the gate-integrity (GIP) adoption as an uncommitted tree over commit `5d0d24d`, including re-verification of owner decisions D1–D5.
**It does not own** product behaviour (`SPEC.md`), the gate contract (`docs/testing-gauntlet.md`), gate floors, the human ROLE-EDITOR copy gate (`F-lessons`), or live counts after later commits.

| Field | Value |
|---|---|
| Revision | Uncommitted tree over `5d0d24d`; snapshot sha256 `5cdec2d1453e1748286375dbda404e51858fd87714d6b0e34d7feb375389d4f8` (119 files via `git ls-files -co --exclude-standard`) |
| Base | `5d0d24d` — Rewrite the six drift parts as agent-usable contracts |
| Reviewer | ROLE-REVIEWER (did not implement) |
| Implementer | ROLE-IMPLEMENTER |
| Date | 2026-08-21 |
| Disposition | Integrate clear: PW-1 (P1) and all fourteen P2 findings corrected and verified by the same reviewer in two passes; P3 findings live as rows in `docs/open-faults.md`; the human ROLE-EDITOR copy gate (`F-lessons`) stays open |

Snapshot recipe (ROLE-IMPLEMENTER): sha256 over the sorted `git ls-files -co --exclude-standard` list, feeding for each path `path + NUL + hex(sha256(file bytes)) + LF`.

## Requirements in scope

- Re-verification of `docs/settled.md` Q-sittings-review-2026-08-21 decisions D1–D5: second-miss hint names the need never the object; transformer gloss leads with `distribution transformer`; the title spec clicks Lights; adult reveal and Show all names run on both sitting specs through one shared helper; e2e steps are real interactions.
- The gate-integrity paper-over scanner per Q-gip-adopt: `tools/check-gate-integrity.mjs`, `tests/gate-integrity.test.ts`, the `check` script composition, `vite.config.ts`, `playwright.config.ts`, and the Agent contract sections of `docs/testing-gauntlet.md`; G-unit, not a 12th gate.
- Mandatory passes: PII (S7); licensing, E10, and the do-not-copy list; ethos client hard-stops (S4, S5, S6).
- E12 proofs for the new player-facing content: JSON Schema plus Ajv tests for `sitting-widen-1`, `sitting-widen-2`, `ui-title-screen`; Playwright specs with teaching, interaction, and regression steps; S9 hit targets.
- Kid-facing copy against S1, S3, S8 and the `docs/level-spine.md` curriculum freeze.
- The six drift parts and governing documents after the slice.

This review was read-only. Gates were not rerun; the numbers below are the implementer's measured results on the reviewed snapshot and are taken as stated.

## Gates (as measured by ROLE-IMPLEMENTER on the reviewed snapshot)

| check | result |
|---|---|
| Vitest | 12 files / 51 cases |
| npm run check | exit 0 |
| npm run gauntlet | exit 0 (tsc; vite build 39.63 kB JS; Playwright 12 passed across chromium-desktop and chromium-mobile in 15.5 s) |
| Floors | live = baseline: vitest_files=12 vitest_cases=51 governing_files=9 history_files=0 playwright_specs=4 schema_files=7 shipped_features=2 declared_features=4 drift_parts=6 |
| File map (G-map) | 119 declared, 119 tracked, 0 problems |
| Gate integrity (G-unit) | 0 problems · 7/7 controls |

What this review cannot see: whether those runs happened on the byte-identical tree (the snapshot hashing recipe is not stated, so the digest was not recomputed); rendered pixel sizes and contrast (derived from CSS and JSON, not measured in a browser); screen-reader announcement order.

## Findings (ROLE-REVIEWER)

Ordered by severity. P1 blocks integrate; P2 should be fixed before any ship claim; P3 is a note.

1. **[P1] PW-1 — Mobile through-line hotspot is covered by a later hotspot (S9).** On the chromium-mobile project (Pixel 5, 393 CSS px) the sitting-2 utility-pole hotspot is almost entirely overlapped by the distribution-transformer hotspot painted after it, leaving roughly a 9 px strip tappable, so a through-line object is effectively unreachable by touch and the mobile spec never clicks it. Evidence (computed, not measured): `src/index.css:12-16` main padding 1.25rem plus `:65-71` 2 px figure border gives a 349 px hotspot layer; `content/sittings/widen-2-lights.json` `obj-utility-pole` layout {x 41.5, y 16.5, w 6.8, h 26} → 24×51 natural, expanded to 44×51 by `button { min-height: 44px; min-width: 44px }` (`src/index.css:27-32`); `obj-distribution-transformer` {x 44.2, y 20.5, w 6.2, h 13} → 22×26 natural, expanded to 44×44; overlap 35×43 of the pole's 44×51 box. `src/app/renderers/BusyBlock.tsx:503-508` `paintedHotspots` keeps JSON order (pole, conductor, transformer); `:1143-1149` renders absolutely positioned buttons with no z-index, so the later transformer sits on top. `e2e/specs/widen-2-lights.spec.ts:36,42,82` click only shop, conductor, transformer. Same mechanism covers `obj-mailbox` 30×41 under `obj-fire-hydrant` (both off-need). Desktop (980 px layer) overlap is 40×72 of a 67×143 pole box; usable there. Affected: `src/app/renderers/BusyBlock.tsx`, `src/index.css`, `content/sittings/widen-2-lights.json`, `e2e/specs/widen-2-lights.spec.ts`, `playwright.config.ts`.

2. **[P2] RV-1 — Sitting 2's second-miss hint keeps the negative clause D1 removed from sitting 1.** `content/sittings/widen-2-lights.json:11` `"secondMissHint": "This is the street object for lights, not the crossing."` versus `content/sittings/widen-1-get-across.json:11` `"This is the street object for getting across."`. The hint is static per sitting (`src/app/WidenSitting1.tsx:49` renders `sitting.secondMissHint` regardless of which off-need was tapped; `src/app/sitting-session.ts:17-20` records only a miss count), so "not the crossing" is a non sequitur for any other miss — `e2e/specs/widen-2-lights.spec.ts:36-41` taps the shop twice and then expects "not the crossing". "The crossing" is also the everyday name for the crosswalk and crossing gates, so the hint names an off-need object rather than only the need, contrary to `docs/settled.md:21` D1 and `docs/level-spine.md:86`. Fix shape: `"This is the street object for lights."`; update `tests/widen-sitting-2.test.ts:23-25` and `e2e/specs/widen-2-lights.spec.ts:39-41` literals; add a `toHaveCount(0)` regression on the old form mirroring `e2e/specs/widen-1-get-across.spec.ts:33`. Affected: `content/sittings/widen-2-lights.json`, `tests/widen-sitting-2.test.ts`, `e2e/specs/widen-2-lights.spec.ts`, `docs/settled.md`.

3. **[P2] COPY-1 — Two drawn objects have no hotspot and no quiet styling.** The far utility pole and the street truck are drawn at full contrast with no hotspot, no name, and no quiet class, breaking the freeze rule that off-need objects stay visually quiet and giving a silent no-op on tap; in Lights the far pole is the same object type as the through-line pole but does nothing. Evidence: `src/app/renderers/BusyBlock.tsx:866` unclassed `<g>` draws `farPole` (`FAR_POLE` at `:515`) while the near pole at `:832` uses `objectShapeClass('obj-utility-pole', hotspots)`; `:1039` unclassed `<g>` draws `TRUCK`. Neither id appears in either sitting's hotspots, so `.quiet-shape` (`src/index.css:80-86`) never applies and no `onOffNeed` miss fires. `docs/level-spine.md` "Off-need objects stay **visually quiet** until their through-line"; `SPEC.md:23` "Off-need objects stay quiet: lower contrast, dashed pattern, not in tab order." Affected: `src/app/renderers/BusyBlock.tsx`, `content/sittings/widen-1-get-across.json`, `content/sittings/widen-2-lights.json`, `src/index.css`, `SPEC.md`.

4. **[P2] GI-1 — `forbid-only-missing` has no negative control (E3).** Deleting the regex at `tools/check-gate-integrity.mjs:106` leaves every gate green. `tools/check-gate-integrity.mjs:168-172` plants `"export default { forbidOnly: true, retries: 1 }"` and asserts only `retries-nonzero`; `:164` plants vite `{ test: {} }` for `allow-only-missing`; no plant omits `forbidOnly`. `tests/gate-integrity.test.ts:50-63` checks only the live config (`playwright.includes('forbidOnly: true')` and `findConfigProblems(...) toEqual([])`), which still passes with the branch removed. The self-test message at `:187` is a hardcoded `'gate-integrity self-test: 7/7 controls'`, not a computed count. Affected: `tools/check-gate-integrity.mjs`, `tests/gate-integrity.test.ts`.

5. **[P2] GI-2 — Live-scan assertion is vacuous with respect to population.** `tests/gate-integrity.test.ts:42-43` `expect(scanRepoPaperOvers()).toEqual([])` passes whether 26 files or zero were scanned. `tools/check-gate-integrity.mjs:118` `listTrackedFiles(root).filter(shouldScanRelativePath)` — the filtered length is never exposed, asserted, or printed (CLI prints only `gate-integrity: N problems`). `shouldScanRelativePath` is declared in `tests/scripts-modules.d.ts:46` but never imported by the unit test, and `runSelfTest` (`:138-176`) never calls it. The drift-check report shape in `.claude/skills/drift-check/SKILL.md:47-64` has no gate-integrity row, so "every coverage number is the whole of its population" cannot be evidenced for this scan. Affected: `tests/gate-integrity.test.ts`, `tools/check-gate-integrity.mjs`, `.claude/skills/drift-check/SKILL.md`.

6. **[P2] GI-3 — "Vitest has no retry" is false and retry evasions are unscanned.** `docs/testing-gauntlet.md:63` "Playwright `retries: 0` (GIP-C8). Vitest has no retry." and `:135` "Already true locally: Playwright `retries: 0`; Vitest has no retry." — but `package.json:34` pins `"vitest": "^4.1.11"` and `node_modules/vitest/dist/chunks/config.d.A1h_Y6Jt.d.ts:147` declares `retry: SerializableRetry;` (config `test.retry` and per-test `{ retry: n }` exist). `tools/check-gate-integrity.mjs:102-112` checks only `allowOnly`, `forbidOnly`, `retries` literals in two config files; `findPaperOvers` (`:66-86`) has no pattern for `retry` or `test.describe.configure({ retries: n })`. R5 / GIP-C8 are therefore evadable by one line the doc does not name as a limit. Affected: `docs/testing-gauntlet.md`, `tools/check-gate-integrity.mjs`.

7. **[P2] SCH-1 — Sitting schemas' safety const, required list, and minLength guards have no negative Ajv control (E3).** `tests/widen-sitting-1.test.ts:46-72` negatives are only `signal!.inTabOrder = false`, `shop!.inTabOrder = true`, `analyticsId: 'nope'`; `tests/widen-sitting-2.test.ts:52-88` adds only `signal!.role = 'through-line'`. `node tools/blast-radius.mjs --word approachLiveGear` lists one test file, `tests/schema-examples.test.ts`, whose control at `:46-57` targets `schema/infrastructure-object-card.schema.json`, not either sitting schema (`schema/sitting-widen-1.schema.json:116-118` `"approachLiveGear": { "type": "string", "const": "never" }` is a separate copy). A future loosening of `approachLiveGear` (S3), `secondMissHint`, `hotspots.minItems`, or kid-facing `minLength` would pass. Affected: `tests/widen-sitting-1.test.ts`, `tests/widen-sitting-2.test.ts`, `schema/sitting-widen-1.schema.json`, `schema/sitting-widen-2.schema.json`.

8. **[P2] SCH-2 — `check-schemas --self-test` controls only `schema-missing` through `findProblems`.** `tools/check-schemas.mjs:73-115`: `runSelfTest` builds `ghostSchema` (only `schema-missing` asserted, `:88`), then calls `validateInstance(titleRow.validates, {...})` directly at `:94` rather than `findProblems`, so a deleted `problems.push({ code: 'instance-invalid' ... })` at `:61` would still pass; `const badTitle` at `:93` is unused (`void badTitle` at `:112`); console prints `'schema inventory self-test: 2/2 controls'` at `:126`. `schema-not-in-code-map`, `schema-without-instance`, `instance-missing`, and `instance-invalid` have no negative control. `docs/testing-gauntlet.md:219` lists three codes but names negative controls only for `schema-missing; wrong title sittingId rejected`. Pre-existing shape, but this revision rewrote the function. A new schema without a code-map row does fail (`:29-33` and `tests/schema-inventory.test.ts:7-15`); nothing enforces that a new schema gets a negative Ajv test. Affected: `tools/check-schemas.mjs`, `docs/testing-gauntlet.md`, `tests/schema-inventory.test.ts`.

9. **[P2] ETHOS-1 — G-ethos has no network or storage token, so S4 has no mechanical guard.** `scripts/lib/client-gates.mjs:1-8` `FORBIDDEN_TOKENS = ['geolocation','getUserMedia','gtag','Sentry','openai','langgraph']` — no `fetch(`, `XMLHttpRequest`, `WebSocket`, `EventSource`, `sendBeacon`, `mediaDevices`, `analytics`, `anthropic`, `@ai-sdk`, `serviceWorker`, `indexedDB`. `docs/testing-gauntlet.md:217` states the gate as that six-token list with blind spot "A new SDK under a name not on the list"; network primitives are not an SDK and are not named as a blind spot. No Playwright spec intercepts requests (`grep -nE 'route\(|network|request' e2e/` → 0 hits). Pre-existing in HEAD, but this revision is the first to ship interactive client code (`src/app/WidenSitting1.tsx`, `src/app/sitting-session.ts`); today's `src/` is clean only by manual grep. Fix is testing-only (E8-safe): extend `FORBIDDEN_TOKENS` and the self-test control, or add a Playwright step asserting zero non-origin requests. Affected: `scripts/lib/client-gates.mjs`, `tests/client-gates.test.ts`, `tools/pii-lint.mjs`, `docs/testing-gauntlet.md`.

10. **[P2] PW-2 — Keyboard focus on hotspots is invisible.** `src/index.css:110-114` `.hotspot-through { outline: 3px solid #12202a; outline-offset: 2px; ... }` and `:116-121` `.hotspot-quiet { outline: 2px dashed #8f8678; ... }` are unconditional author `outline` rules, which override the UA `:focus-visible { outline: auto }`; `grep focus src/index.css` returns nothing. Tabbing across the three through-line buttons (`tabIndex 0`, `BusyBlock.tsx:1148`) produces no visual change. Show all names and Back keep the UA ring because nothing styles their outline. Affected: `src/index.css`, `src/app/renderers/BusyBlock.tsx`.

11. **[P2] PW-3 — Tab-order contract is proven only at the JSON layer.** `SPEC.md:23` and `docs/level-spine.md:26` state through-line first in tab order and off-need not in tab order; `BusyBlock.tsx:1148` `tabIndex={hotspot.inTabOrder ? 0 : -1}` is the only realisation. `tests/widen-sitting-1.test.ts:25-63` and `tests/widen-sitting-2.test.ts:41-70` assert `inTabOrder` on the JSON objects only. `grep -rn "tabIndex|tabindex|toBeFocused|keyboard|Tab\b" e2e/ tests/` returns no hits outside planted strings in `tests/gate-integrity.test.ts`. `tabIndex={0}` for every hotspot would pass every gate. Affected: `e2e/specs/widen-1-get-across.spec.ts`, `e2e/specs/widen-2-lights.spec.ts`, `src/app/renderers/BusyBlock.tsx`, `tests/widen-sitting-1.test.ts`, `tests/widen-sitting-2.test.ts`.

12. **[P2] PW-4 — Through-line name labels are clipped inside their hotspot boxes.** `src/index.css:101-108` `.hotspot { ... overflow: hidden; }`; `:123-133` `.hotspot-name { display: block; font-size: 0.75rem; font-weight: 600; padding: 0.15rem 0.35rem; max-width: 100%; }`. The transformer hotspot (`widthPercent 6.2`) is ~61 px on the 980 px desktop layer and 44 px on Pixel 5, leaving ~50 px / ~33 px inner width; "Distribution" (12 glyphs at 12 px bold serif) cannot fit and cannot break. Traffic signal (27 px natural on mobile) and Crossing gates are clipped on mobile the same way. `e2e/specs/widen-2-lights.spec.ts:26-28` asserts `.hotspot-name` `toHaveText('Distribution transformer')`, which reads textContent and is satisfied by clipped text, so the "named through-line" promise is not legible on the block. Affected: `src/index.css`, `src/app/renderers/BusyBlock.tsx`, `e2e/specs/widen-2-lights.spec.ts`, `content/sittings/widen-2-lights.json`.

13. **[P2] PW-5 — Hit-target test is a string-presence check with no link to rendered hotspots and no negative control (E3, S9).** `tests/hit-target.test.ts:7-8` `expect(css.includes('min-height: 44px')).toBe(true)` / `min-width: 44px` match any occurrence anywhere in `src/index.css`; `:10-11` assert `<svg` present and no `webgl`, which is not a hit-target check. `grep -rn boundingBox e2e/` returns nothing. Real coverage comes only from `src/index.css:27-32` `button, a, [role='button'] { min-height: 44px; min-width: 44px }` applying to `<button class="hotspot ...">` at `BusyBlock.tsx:1143`; nothing asserts that linkage, so `.hotspot { min-width: 0 }` would pass green. `docs/testing-gauntlet.md` lists no negative control for TEST-HIT-TARGET. Affected: `tests/hit-target.test.ts`, `src/index.css`, `src/app/renderers/BusyBlock.tsx`.

14. **[P2] DRIFT-01 — Four documents call sittings 1–2 "shipped" while D2 says the copy gate comes before "shipped".** `CLAUDE.md:8` "This slice ships guard rails plus widen sittings 1–2"; `README.md:12` "This slice ships the title plus **widen sittings 1–2**"; `SPEC.md:23` status "Sittings 1–2 shipped"; `docs/feature-map.md:41` status "shipped" — versus `docs/settled.md:21` "D2 the human ROLE-EDITOR copy gate stays ahead of \"shipped\" (`F-lessons`)"; `docs/open-faults.md:10` "ROLE-EDITOR human gate still required"; `:18` "integrate stays blocked until then or an owner exception"; `workflows/README.md:7` "Do not treat those sittings as copy-complete until ROLE-EDITOR records a pass". The only hedge, `docs/feature-map.md:33` "Cannot see ... That kid-facing copy passed the human gate", is a disclaimer, not a definition. No document defines the feature-map `shipped` label as a lockstep status distinct from the copy gate. Fix: one sentence in `SPEC.md` or the feature-map MUST list defining `shipped` = built, in lockstep, Playwright-covered, copy gate tracked by `F-lessons`; or reword `CLAUDE.md` / `README.md` to "builds". Affected: `CLAUDE.md`, `README.md`, `SPEC.md`, `docs/feature-map.md`, `tools/feature-map.mjs`, `docs/settled.md`, `docs/open-faults.md`, `workflows/README.md`.

15. **[P2] DRIFT-02 — `docs/level-spine.md` still says "Unbuilt" and "Do not build yet".** `docs/level-spine.md:6` "Closed from the 2026-08-18–19 owner questionnaire and the 2026-08-19 civil-expert interview. Unbuilt. Not a lesson catalog."; `:4` "Do not build Learn levels until this file is the brief."; `:163` "## Do not build yet ... until object cards and schemas follow this spine" — versus `README.md:49` "(frozen; sittings 1–2 follow it)", `docs/open-faults.md:13` "Widen #1–2 are playable; sittings 3–11 and deepen remain unbuilt", `SPEC.md:23`. The diff touched this file (art lines) but left the status sentence: a stale paragraph in fresh words. Affected: `docs/level-spine.md`, `README.md`, `docs/open-faults.md`, `SPEC.md`.

16. **[P3] COPY-2 — SVG assistive description drifts from on-screen names and leaks dev jargon.** `src/app/renderers/BusyBlock.tsx:545` `aria-label="An isometric 16-bit town block with a shop, a house, jobs, a street, a utility pole, a transformer, overhead wires, a crosswalk, a traffic signal, and railroad tracks."` versus `content/sittings/widen-2-lights.json:45` `"displayName": "Overhead conductor"` and `:60` `"Distribution transformer"`; omits mailbox, fire hydrant, crossing gates; "16-bit" is not kid-facing copy. `docs/level-spine.md`: "Put the **real name on screen**". Affected: `src/app/renderers/BusyBlock.tsx`, `content/sittings/widen-2-lights.json`.

17. **[P3] GI-4 — Prohibited category B overstates scanner coverage.** `docs/testing-gauntlet.md:75` binds "an expected-failure mark ... a test with no assertion" to `skip-mark`, `only-mark`, `vacuous-expect`, but `tools/check-gate-integrity.mjs:11-22` define only `.skip`, `.only`, `xit`, `xdescribe`; no pattern for `it.fails(`, `test.fail(`, `test.fixme(`, `it.todo(`, and `it\.skip\s*\(` (`:24-26`) does not match `it.skipIf(true)(`. Affected: `docs/testing-gauntlet.md`, `tools/check-gate-integrity.mjs`.

18. **[P3] GI-5 — Config checks are substring regexes over whole-file text.** `tools/check-gate-integrity.mjs:103` `/\ballowOnly\s*:\s*false\b/.test(text)`, `:106` `/\bforbidOnly\s*:\s*true\b/`, `:109` `/\bretries\s*:\s*0\b/` match a commented-out `// retries: 0` beside a live `retries: 2`; `shouldScanRelativePath` (`:39-60`) names only `vite.config.ts` and `playwright.config.ts`, so a `vitest.config.ts` (which takes precedence) would not be looked at. `tests/gate-integrity.test.ts:53-55` uses the same `includes(...)` checks. Affected: `tools/check-gate-integrity.mjs`, `tests/gate-integrity.test.ts`.

19. **[P3] GI-6 — Only one alternative of each multi-pattern detector is controlled.** `tools/check-gate-integrity.mjs:140` plants `SKIP_CALLS[0]` only; `:144` `ONLY_CALLS[0]` only; `:155` `vitest run || process.exit(0)` only; `tests/gate-integrity.test.ts:12,17,36` plant `it.skip`, `test.only`, `process.exit(0)`. `test.skip`, `describe.skip`, `xit`, `xdescribe`, `describe.only`, `|| true`, `|| exit 0`, and `VACUOUS_SCRIPT_RE` (`:37`) have no plant and can be deleted silently. Affected: `tools/check-gate-integrity.mjs`, `tests/gate-integrity.test.ts`.

20. **[P3] GI-7 — Skip/only call patterns lack a leading word boundary.** `tools/check-gate-integrity.mjs:24-26` `callPattern(name)` builds `${name}\s*\(` with no `\b`, versus `:30-31` which give `xit`/`xdescribe` a `\b`; identifiers ending in `it`/`test`/`describe` (`emit.skip(`, `latest.only(`) and comment prose would be reported. No current hit in the tree; scanner and unit test already evade themselves via `['it', '.skip'].join('')`. Affected: `tools/check-gate-integrity.mjs`.

21. **[P3] GI-9 — forced-success on package.json scripts is narrow.** `tools/check-gate-integrity.mjs:35-37` `FORCED_SUCCESS_RE = /\|\|\s*(?:true\b|exit\s+0\b|process\.exit\s*\(\s*0\s*\))/` and `VACUOUS_SCRIPT_RE` miss `|| echo ok`, `|| :`, `; exit 0`, and vitest `--passWithNoTests` (`node_modules/vitest/dist/chunks/config.d.A1h_Y6Jt.d.ts:86`), which is always-green when the include glob matches nothing; `docs/testing-gauntlet.md:80` binds category F to this detector without naming the limits. Affected: `tools/check-gate-integrity.mjs`, `docs/testing-gauntlet.md`.

22. **[P3] SCH-4 — Schemas pin an incomplete curriculum.** `schema/sitting-widen-1.schema.json:137-168` `contains` blocks pin `obj-traffic-signal`, `obj-crosswalk` (through-line) and `obj-shop` (off-need) only, though `docs/level-spine.md:113` names rail crossing as the third Get-across object; `schema/sitting-widen-2.schema.json:137-197` pins only `obj-traffic-signal` and `obj-shop` as off-need, so moving `obj-crosswalk` or `obj-crossing-gates` onto the Lights through-line validates despite `tests/widen-sitting-2.test.ts:80-88` titling its case "rejects a crossing object placed on the Lights through-line" while flipping only `obj-traffic-signal`. The sitting-1 gap is caught by the literal name assertion at `tests/widen-sitting-1.test.ts:27` via the src constant, not via the schema. Affected: `schema/sitting-widen-1.schema.json`, `schema/sitting-widen-2.schema.json`, `tests/widen-sitting-2.test.ts`.

23. **[P3] SCH-5 — Hotspot ids are not required to be unique.** `schema/sitting-widen-1.schema.json:129-135` `"hotspots": { "type": "array", "minItems": 5, "maxItems": 16, "items": {...} }` with no `uniqueItems` or id-uniqueness rule (same in widen-2); no test checks uniqueness; `src/app/renderers/BusyBlock.tsx:1144` `key={hotspot.id}` would collide and `src/app/sitting.ts:51` `find((hotspot) => hotspot.id === id)` resolves to the first match. Affected: `schema/sitting-widen-1.schema.json`, `schema/sitting-widen-2.schema.json`, `src/app/sitting.ts`, `src/app/renderers/BusyBlock.tsx`.

24. **[P3] PII-1 — Rewritten G-pii scanner silently drops unreadable candidates and does not decode git's C-quoted paths.** `scripts/lib/pii-scan.mjs:163-170` `try { text = readFileSync(path.join(root, relativePath), 'utf8') } catch { continue }`; `:104-110` `parsePorcelainPath` strips surrounding quotes but leaves `\303\251`-style escapes, so a quoted `?? "…"` status line yields a path that fails to open and is skipped with no signal. HEAD's `scripts/scan-pii.mjs` / `tools/pii-lint.mjs` used `await readFile(...)` with no catch (it threw). `docs/testing-gauntlet.md:77` names class D paper-over as "add a file to an ignore list to hide a finding"; a silent skip is the same shape. Suggest counting skipped files and failing (or printing) when > 0, plus a negative control with a constructed unreadable or quoted path. Affected: `scripts/lib/pii-scan.mjs`, `tests/pii-scan.test.ts`, `tools/pii-lint.mjs`.

25. **[P3] PII-2 — Pre-existing, not in this diff: the configured git identity email is a GitHub noreply address under a handle that is not the `opencutaway` username `docs/PRIVACY.md` prescribes, and every commit in history carries it.** `docs/PRIVACY.md:26` "use `git config user.name opencutaway` and a GitHub noreply identity in the form GitHub documents for that username." `git config user.name` → `opencutaway`; `git config user.email` and `git log -5 --format='%ae'` → a noreply address whose local-part is a different handle (not quoted here). Not detectable by G-pii, which scans file contents only. Future commits should use the documented identity; history rewrite is the owner's call. Affected: `docs/PRIVACY.md`.

26. **[P3] PW-7 — Reduce-motion is proven only by CSS string presence.** `tests/widen-sitting-1.test.ts:104-110` `expect(css.includes('@media (prefers-reduced-motion: reduce)')).toBe(true)` and `expect(css.includes('animation: none')).toBe(true)`; `src/index.css:160-170` defines the rules correctly, but no spec calls `page.emulateMedia({ reducedMotion: 'reduce' })` and checks the computed animation. Affected: `tests/widen-sitting-1.test.ts`, `src/index.css`, `e2e/specs/widen-1-get-across.spec.ts`.

27. **[P3] PW-8 — `trace: 'on-first-retry'` with `retries: 0` never records a trace.** `playwright.config.ts:16` `retries: 0`, `:23` `trace: 'on-first-retry'`; a failing e2e run leaves screenshots as the only evidence. `'retain-on-failure'` gives a trace without re-running (R5 forbids retry to green). Affected: `playwright.config.ts`.

28. **[P3] PW-9 — Session state is not keyed per sitting across hash navigation.** `src/app/App.tsx:40-46` returns `<WidenSittingScreen sitting={WIDEN_SITTING_1|2} ...>` from the same render position with no `key`, and `:24-28` listens to `hashchange`; `src/app/WidenSitting1.tsx:26` `useState(createSittingSession)` is never reset on prop change, and `:27` `hotspotById(session.foundId, sitting)` resolves `'obj-traffic-signal'` in sitting 2 as an off-need hotspot with a displayName, so a direct change from `#/get-across` to `#/lights` can show the Traffic signal card inside Lights. All e2e specs reach sittings only through the title buttons and Back. Affected: `src/app/App.tsx`, `src/app/WidenSitting1.tsx`.

29. **[P3] DRIFT-03 — CHANGELOG contradicts itself and adds headings for states that never existed as commits.** `CHANGELOG.md:15` "- No playable Learn/Challenge lessons yet." immediately followed by `:16` "Widen sittings 1–2 (Get across, Lights) are the first playable sittings from that freeze."; headings `:18` "## 0.0.0+sitting-1" and `:25` "## 0.0.0+sitting-2" describe two slices, but `git log --oneline` shows no sitting-1 commit (HEAD `5d0d24d`) and `docs/settled.md:21` D6 says sittings land as the next commit (singular); `:29` "Busy-block placeholders redrawn" describes an uncommitted intermediate (`src/app/renderers/BusyBlock.tsx` is untracked). `CHANGELOG.md:3` owns "parent-facing history of repository slices". Affected: `CHANGELOG.md`.

30. **[P3] DRIFT-05 — Hint asymmetry and Show-all-names asymmetry across sittings.** `content/sittings/widen-2-lights.json:11` names "the crossing" while `tests/widen-sitting-1.test.ts:32` `expect(WIDEN_SITTING_1.secondMissHint.includes('shop')).toBe(false)` and `CHANGELOG.md:23` "without naming shop or which object to tap" encode the rule that the hint must not name an off-need object. Sitting 1's 8 hotspots omit `obj-overhead-conductor` and `obj-distribution-transformer`, which `src/app/renderers/BusyBlock.tsx:888,897` draw with `objectShapeClass(...)`; sitting 2 has 10 hotspots covering every drawn object, so `SPEC.md:23` / `docs/level-spine.md:28` "show all names" names 8 of 10 drawn objects in sitting 1. Copy is pending the ROLE-EDITOR gate, hence P3. Affected: `content/sittings/widen-2-lights.json`, `content/sittings/widen-1-get-across.json`, `tests/widen-sitting-1.test.ts`, `src/app/renderers/BusyBlock.tsx`, `SPEC.md`.

31. **[P3] DRIFT-06 — Decision-page format and "silence is not approval" restated in the gauntlet.** `node tools/blast-radius.mjs --word "copy-all at the end"` → `AGENTS.md`, `CLAUDE.md`, `docs/testing-gauntlet.md`; `docs/testing-gauntlet.md:95` repeats `CLAUDE.md:16` verbatim; `--word "Silence is not approval"` → `AGENTS.md:68` and `docs/testing-gauntlet.md:68,95`. `CLAUDE.md:3` owns the scan style for chat replies; the gauntlet should point, not restate. Affected: `docs/testing-gauntlet.md`, `AGENTS.md`, `CLAUDE.md`.

32. **[P3] DRIFT-08 — Gate count restated in wordings the copied-fact detector cannot see; F-gip-github duplicates gauntlet status cells.** `tools/file-map.mjs:60` shape regex is one literal sentence only (the owner's exact wording; not reproduced here because quoting it trips the detector). Restatements: `docs/testing-gauntlet.md:41` "The eleven named gates", `:125` "FACT-GATE-COUNT stays eleven named gates ... Do not add a 12th named gate", `:207` "not a 12th named gate"; `.cursor/rules/drift-maps.mdc:8`; `docs/settled.md:22` "not a 12th gate"; `tools/file-map.mjs` OWN-GATE-INTEGRITY note (→ `docs/file-map.md` twice); `node tools/blast-radius.mjs --word 12th` → 8 files. `docs/open-faults.md:17` repeats `docs/testing-gauntlet.md:115-116` GIP-C1/C2 status. Affected: `docs/testing-gauntlet.md`, `.cursor/rules/drift-maps.mdc`, `docs/settled.md`, `tools/file-map.mjs`, `docs/file-map.md`, `docs/open-faults.md`.

33. **[P3] DRIFT-09 — Gauntlet cites an external draft policy's section numbers, and the drift-check report shape lacks a gate-integrity row.** `docs/testing-gauntlet.md:72` "### Prohibited paper-over (§6 A–G)", `:56` "### R1–R8 (mapped to this toolchain)", `:108` "Draft-policy control IDs" — no file in the tree defines §6, R1–R8, or the draft (grep for `§6` hits only this file). `:82` binds category G to the drift-check report, but `.claude/skills/drift-check/SKILL.md:36` adds `node tools/check-gate-integrity.mjs` to the run list while the report table (`:48-64`) has no row for its numbers. Affected: `docs/testing-gauntlet.md`, `.claude/skills/drift-check/SKILL.md`.

34. **[P3] DRIFT-10 — SPEC's FEAT-LEARN row omits the reveal precondition the code enforces, and FEAT-TITLE status differs from README.** `SPEC.md:23` "The adult can reveal and can toggle show all names." — `src/app/sitting-session.ts:48` `shouldShowAdultReveal = shouldShowHint(session) && !session.revealed` and `src/app/WidenSitting1.tsx:66` render the reveal button only after two misses, matching `docs/level-spine.md:129` "Then the adult can reveal" but not SPEC. `SPEC.md:22` FEAT-TITLE "Placeholder shipped" versus `README.md:16` "Title | Shipped: ..." and `docs/feature-map.md:40` "shipped". Affected: `SPEC.md`, `README.md`, `src/app/sitting-session.ts`, `src/app/WidenSitting1.tsx`.

## Passes

All passes were by reading and grep over the snapshot; gates were not rerun.

**D1–D5 re-verification**
- D1 content: `content/sittings/widen-1-get-across.json:11` is exactly "This is the street object for getting across."; no through-line or off-need displayName appears in it.
- D1 unit test (E2): `tests/widen-sitting-1.test.ts:29-31` asserts an inline string literal; `:32-35` add negative `includes()` checks for `shop` and the three through-line names.
- D1 e2e: `e2e/specs/widen-1-get-across.spec.ts:27-41` clicks the shop twice, asserts "Try again.", the literal hint, `toHaveCount(0)` on the old "not the shop" form, then clicks the signal and asserts name/gloss/function.
- D3: `content/sittings/widen-2-lights.json:61` gloss leads with "distribution transformer; it steps voltage down ..."; `tests/widen-sitting-2.test.ts:34-36` literal; `e2e/specs/widen-2-lights.spec.ts:81-89` clicks the transformer and asserts h2 and gloss.
- D4: `e2e/specs/title.spec.ts:24-30` backs out of sitting 1, clicks `lightsControl()`, asserts sitting-2 heading and prompt; App wiring at `src/app/App.tsx:44-46,56-58`.
- D5: both sitting specs import `e2e/helpers/adult-sitting-controls.ts` and wrap teaching / interaction / regression steps (`:62-72`, `:69-79`); the helper asserts reveal absent, Show all names round-trips `aria-pressed`, reveal appears after two misses and disappears after use, exact revealed-names string, no found h2, no Challenge heading, no timer text.
- Real interactions: `BusyBlock.tsx:1144-1158` renders `<button data-hotspot>` dispatching `onThroughLine`/`onOffNeed`; `WidenSitting1.tsx:48-59` renders hint/reveal/card only from session state; `sitting-session.ts:37-47` gates hint on `misses >= 2 && found === null`.
- Playwright 12/12 is consistent: `grep -c '^\s*test(' e2e/specs/*.spec.ts` → 1+1+2+2 = 6 cases × 2 projects.
- Schemas do not hard-code hint or gloss strings (type string, minLength 1, maxLength 240 / 200); literals are pinned by unit tests.

**Kid-facing copy (S1, S3, S8, curriculum freeze)**
- Real name first plus gloss ≤ 200 chars on every through-line; off-need names are real objects; `tryAgain` is "Try again." with no shame wording; grep for baby-talk and exclamation tokens over `content/` and `src/app/` → none.
- No banned nouns (`dam`, `hub`, `big sewer pipe`, `stoplight`, `landfill`, `power line`) in kid-facing strings; D3 not reopened.
- S3: both sittings `safety.approachLiveGear = "never"`, schema const; `safety.notes` forbid sending anyone toward live gear; prompts act on the drawing only.
- S8: no unlock, album, life list, star, score, or Challenge text; `grep -rn "score|streak|star|lock|points" src` → only "block".
- `sitting-session.ts`: no timer, lockout, or score; `grep -rn "setTimeout|setInterval|Date\.|performance\." src` → none.
- Quiet ≠ color-only for hotspot objects: `contrast: low` + `pattern: dashed-quiet` + `inTabOrder: false` in content; `.quiet-shape` applies filter and `stroke-dasharray`; `.hotspot-quiet` dashed outline; reduce-motion removes the pulse.
- No other kid-visible string sources: `index.html` title "Open Cutaway"; `src/index.css` has no `content:` strings; `BusyBlock.tsx` text nodes are `hotspot.displayName` and the SVG `aria-label` (COPY-2).

**Gate-integrity scanner**
- `skip-mark`, `only-mark`, `vacuous-expect`, `forced-success`, `allow-only-missing`, `retries-nonzero` each have at least one negative control in self-test (`:139-173`) and unit test (`:11-40`); `forbid-only-missing` is the one code without (GI-1).
- No false positive in the tree: `grep -rnE "\.(skip|only|todo|fails|fixme|fail|skipIf|runIf|configure)\b|retry|retries" tests e2e scripts` hits only the scanner and its test, which evade via string joins; the `toBeTruthy` / `expect(value).toBe(true)` false-positive control exists in both paths.
- Population: `tests/`, `e2e/` (including `helpers/`, `pages/`), `scripts/` for `.ts/.tsx/.js/.mjs/.cjs` plus `package.json`, `vite.config.ts`, `playwright.config.ts`; vitest `include` and Playwright `testDir` agree with the scanned roots; source is `git ls-files -co --exclude-standard` (119 files), so the 19 untracked files are scanned.
- FACT-GATE-COUNT is 11 and the Gates table has exactly 11 rows; the scanner is described as G-unit / not a 12th gate consistently across `docs/testing-gauntlet.md`, `.cursor/rules/drift-maps.mdc`, `AGENTS.md`, `docs/file-map.md`, `tests/registry.json`.
- Live configs: `vite.config.ts:10` `allowOnly: false`; `playwright.config.ts:16-17` `forbidOnly: true`, `retries: 0`; `package.json` `check` is a pure `&&` chain containing `node tools/check-gate-integrity.mjs` and `--self-test` after `vitest run`.
- Ownership: OWN-GATE-INTEGRITY kind GATE in `tools/file-map.mjs:120`; effect-map rows for all 7 unit cases; `docs/open-faults.md` F-gip-review names this review. `node tools/blast-radius.mjs --word gate-integrity` → 15 files.

**E12 schema + Ajv**
- `additionalProperties: false` at every object level in all three schemas; `required` lists complete (17 top-level sitting keys, 6 hotspot keys, through-line `if/then` requires gloss and functionSummary, 7 title keys); `const`/`enum` wherever the curriculum fixes a value; `rung: 1` correct per `docs/level-spine.md:102`; `minLength: 1` plus a cap on every kid-facing string.
- Each schema has a positive Ajv test on the committed instance and at least one negative (`tests/widen-sitting-1.test.ts`, `tests/widen-sitting-2.test.ts`, `tests/schema-inventory.test.ts`).
- E2: expected values are literals; content JSON is read only as subject, never as oracle.
- `diff schema/sitting-widen-1.schema.json schema/sitting-widen-2.schema.json` differs only in `$id`/title/description, three identity consts, and the `contains` list.
- `tools/check-schemas.mjs:28-33,50-52` fail an unmapped schema or a row with no instance; `tests/schema-inventory.test.ts:7-15` literal list would also fail.
- Ajv is strict and offline (`scripts/lib/ajv-validate.mjs:25-30`); only `$ref` is local; no parallel parser beside JSON Schema (`src/app/sitting.ts:1-2` imports the JSON with `with { type: 'json' }`).

**PII / licensing / ethos**
- PII grep over all 60 dirty and untracked files for email patterns, user-folder paths, drive-letter paths, home-dir paths, and owner/provider handles → zero hits; capitalised two-word tokens in new `src`/`content`/`e2e`/`schema`/`tests` are product terms only; sample names appear only as sanctioned `Player A` / `Pat` / `Jordan` and constructed test needles.
- `e2e/fixtures/player.ts` diff adds only two page-object fixtures; `.gitignore` still covers `local/`, `profiles/*.json`, `*.save.json`; `grep -rniE 'localStorage|sessionStorage|indexedDB' src/` → 0.
- `LICENSE` is MIT, "Open Cutaway authors", untouched; `package.json` diff touches only the `check` script; `dependencies` = preact, `devDependencies` unchanged (E10).
- `BusyBlock.tsx` (1164 lines) is hand-authored procedural SVG: `isoPt()` coordinates, rect/polygon/ellipse/path from numbers, five tiny `<pattern>`s, inline hex palette; no `<image>`, no `data:`/base64, no `@font-face`, no `<use>`/`<symbol>` of imported art, no path `d` > 80 chars; system fonts only; no binary files added.
- Do-not-copy: `docs/inspiration.md` diff widens the ban to SNES tiles/characters/maps/UI/fonts/palettes and ROM rips; `grep -rniE 'simcity|snes|nintendo|scarry|busytown|macaulay|zelda|chrono|harvest moon|earthbound|lowly|huckle' src content e2e schema` → 0.
- Ethos hard-stops: `grep -rniE 'fetch\(|XMLHttpRequest|WebSocket|EventSource|sendBeacon|geolocation|getUserMedia|mediaDevices|analytics|gtag|sentry|openai|anthropic|@ai-sdk|localStorage|sessionStorage|indexedDB|caches\.|serviceWorker|navigator\.share|importScripts|https?://' src/ index.html vite.config.ts` → exit 1; state is in-memory Preact plus `location.hash`; adult reveal is a plain button (S2); content JSON has no URLs or image refs.
- G-ethos negative control builds `getUserMedia` from fragments in `tests/client-gates.test.ts:8-11` and `tools/pii-lint.mjs:33-36`; G-pii negative controls (email, Windows path, macOS path, clean-text false positive) present in vitest and `--self-test`.
- PII scanner rewrite keeps population equivalent to HEAD (`git ls-files -z` plus `git status --porcelain --untracked-files=all --ignored=no`); new ignore entries (`node_modules`, `dist`, `coverage`, `.vite`) are already gitignored.

**Playwright / S9**
- Every spec has `test.step` labels prefixed `teaching:` / `interaction:` / `regression:`; no `waitForTimeout`, `.only`, `.skip`, `fixme` in `e2e/`; `forbidOnly true`, `retries 0`, `workers 1`, `reuseExistingServer false`.
- Assertions target accessible state (headings by role/level, buttons by role and name, `aria-pressed`, `role=status`); the only DOM hooks are `[data-hotspot]` and `.hotspot-name`.
- Hotspots are real `<button type=button>` with an accessible name in named and quiet states; SVG art has `role=img` with an `aria-label`.
- Rendered `tabIndex` mapping matches the JSON contract and no focusable element precedes the through-line buttons (`WidenSitting1.tsx:31-36` header markup).
- The `button, a, [role='button']` 44 px rule applies to title buttons, sitting controls, and hotspot buttons; on desktop (980 px layer) all natural hotspot boxes are already ≥ 44 px.
- Reduce-motion: pulse only under no-preference, `animation: none` under reduce; keyframes use `box-shadow`, which is not clipped by `overflow: hidden`.
- Pixel 5 layout should not scroll horizontally (right-most expanded hotspot ends at ~267 px of the 349 px layer; flex controls wrap).
- D3 regression literal guarded in `e2e/specs/widen-2-lights.spec.ts:81-89`.

**Six drift parts and governing docs**
- SPEC FEAT-LEARN through-line names and counts match content; all through-line `inTabOrder: true`, `contrast: high`, `pattern: solid`; all off-need `inTabOrder: false`, `contrast: low`, `pattern: dashed-quiet`.
- Code matches the SPEC flow (try-again on miss 1, hint on miss ≥ 2, reveal after hint, show-all-names toggle, back to title; hash routes `#/get-across`, `#/lights`).
- D3/D4/D5 true on disk (grep of gloss, `lightsControl`, shared helper imports).
- CHANGELOG sitting-2 bullets verified against the tree (both title buttons always present; gloss order; job sprites drawn with no hotspot; SVG, no WebGL).
- New rows sit in the right document: Q-check-stall, Q-sittings-review, Q-gip-adopt are closed with evidence in `docs/settled.md`; F-gip-github, F-sittings-reverify, F-gip-review are open with where/done-means in `docs/open-faults.md`; `docs/reviews/**` is a declared owner glob (`tools/file-map.mjs:110-111`). Q-check-stall's 5 s vitest wall and 2 s timeout match vitest's default and `scripts/lib/pii-scan.mjs:8`.
- `docs/feature-map.md` FEAT-LEARN lists both sitting specs, schemaIds CODE-WIDEN-SITTING-1/2, entry `src/app/WidenSitting1.tsx`; `docs/tutorial-manifest.md` TUT-OBJECT-NAME-FUNCTION taught by sittings 1–2, TUT-CHAIN-PATH and TUT-LIFE-LIST deferred; produces/consumes paths exist.
- Gate baseline consistent with the effect map and tree: 51 cases over 12 files, playwright_specs 4, schema_files 7, shipped_features 2; no floor lowered; `history_files_max` ceiling kept.
- Hit-target size: CLAUDE.md S9 owns 44 px; SPEC and inspiration repeat it only as pointers. "Finished work" stated only in CLAUDE.md. Drift-check SKILL and `.cursor` rule add the gate-integrity command consistently with `package.json` `check` and the gauntlet cadence.

## Refuted on verification

Candidate findings reproduced at the cited lines but judged not actionable under the governing rules; recorded so the trail is honest.

- COPY-3 (reveal button lacks an adult cue): the label follows the spine's "Then the adult can reveal" as an adult-co-play control (S5); no rule requires the label to say "adult", and the consequence (child skipping the teach) is the honor-system design S2 already accepts.
- COPY-4 (sitting-1 hint restates the prompt): D1 requires the hint to name the need and never the object; restating the need is within the decision, and suggesting a need to rule out is an editorial preference for the ROLE-EDITOR gate, not a defect.
- COPY-5 ("Progress stays on this device" with no persistence): the sentence is a privacy promise owned by `SPEC.md` and true (nothing leaves the device); wording belongs to the owner, not to ROLE-IMPLEMENTER.
- GI-8 (unit-test title says "tracked" while population is `ls-files -co`; gitignored `tmp/` test dirs outside scan but inside runner globs): the population is the stricter superset, the gauntlet row does not claim "tracked", and no such `tmp/` tests exist; wording only.
- SCH-3 (sitting schemas are copy-paste twins): `diff` confirms identical shared blocks; no drift exists today and no rule bans duplicated schema `$defs` (the copied-fact rule covers owned prose facts, not schema structure).
- PW-6 (regression steps guard strawman literals that never shipped): `git log -S` confirms the literals never existed, but the gauntlet's regression-step contract asks for a guard against a named fault class, not only against a previously committed string; the D1 old form is guarded in sitting 1 (sitting 2's gap is RV-1).
- DRIFT-04 (second-miss rule stated in four wordings): the quotes exist (one citation was at `docs/level-spine.md:86`, not `:129`), but each is a pointer-length paraphrase in a document that owns a different fact (decision, changelog, spec status), not a copied owned-fact shape.
- DRIFT-07 (spec file list hard-coded in four places): reproduced in `AGENTS.md:43`, `e2e/README.md:12`, `docs/testing-gauntlet.md:196`, `tools/feature-map.mjs`; G-lockstep checks the generated one and the prose copies are each in that document's own scope; a sitting-3 change would be caught by lockstep, so this is maintenance cost rather than a fault.

## Rendered measurements after review (ROLE-IMPLEMENTER, 2026-08-21)

Measured in headless Chromium against the freshly built `dist/` on both Playwright projects, by `getBoundingClientRect` plus an `elementFromPoint` grid (20 × 20) per hotspot. These confirm the findings the reviewer derived from CSS and JSON.

| Finding | Measurement |
|---|---|
| PW-1 | chromium-mobile (393 px) `#/lights`: `obj-utility-pole` box 44 × 51 px, 28% of it exposed, widest tappable horizontal run 30 px. Desktop: 70% exposed. `obj-mailbox` 33% exposed with a 13 px widest run on both mobile routes; `obj-crossing-gates` 35% on mobile Lights |
| PW-2 | Programmatic focus on a through-line hotspot leaves the computed outline unchanged at `rgb(18, 32, 42) solid 3px` on all four project × route combinations; `:focus-visible` matches but nothing renders differently |
| PW-4 | `Distribution transformer` label scrollWidth 73 px in a 63 px (desktop) / 44 px (mobile) box; mobile also clips `Crosswalk` 66 > 56 px and `Crossing gates` 58 > 44 px |
| PW-5 | Every hotspot box is at least 44 × 44 px (the CSS minimum holds), so the string-presence test is satisfied while the effective tappable areas above fall below it |
| Mobile overflow | `document.documentElement.scrollWidth` equals the 393 px viewport on both routes: no horizontal scroll |

## Correction verification

Same-reviewer verification by ROLE-REVIEWER of the corrected tree, 2026-08-21, in two passes. The first pass verified the corrections to every finding; the second pass verified the implementer's response to each PARTIAL residue of the first, on the final tree (snapshot sha256 `70f4681614f4639561e1eba13ae3c6c2f8d0eb8f17748db974556cbe3360b9f8` over 122 files, review recipe). Both passes were read-only: files and diffs were inspected, unit tests and tools were re-executed where cited (`vitest`, `node tools/*.mjs`, planted faults on scratch copies and in memory, repository untouched); Playwright was not re-run, so every Playwright number below is the implementer's gauntlet run on the final tree, taken as stated. CORRECTED means the defect is gone and a proof on disk goes red on its return; PARTIAL means the defect is reduced but a named residue remains; NOT_CORRECTED would mean the defect stands.

| Finding | Final verdict | Evidence (file:line) | Remaining |
|---|---|---|---|
| PW-1 (P1) | CORRECTED | `content/sittings/widen-2-lights.json` transformer box {50.6, 20.5, 10.4, 14.5} no longer intersects the pole; pairwise scan of 8 + 10 boxes at the 720 px rule finds zero intersections, smallest side 44.5 px; `src/app/sitting.ts:55-63,72-109` `HOTSPOT_LAYOUT_RULE` / `findHotspotLayoutProblems`; `tests/hotspot-layout.test.ts` live sittings `[]` against a test-local literal rule, planted pre-fix boxes yield the literal overlap record; `src/index.css:65-75` `.busy-block-scroll` overflow-x auto and `.busy-block { min-width: 720px }`; `e2e/helpers/hotspot-reach.ts:7-31` 5×5 `elementFromPoint` grid asserting width ≥ 44, height ≥ 44, exposed `toBe(1)` on every id at `e2e/specs/widen-1-get-across.spec.ts:99` and `e2e/specs/widen-2-lights.spec.ts:123` on both projects; trade-off stated at `SPEC.md:23`, `CHANGELOG.md:33`, `docs/inspiration.md:35` | None. First-pass notes closed: inspiration wording matches the panning behaviour; `scripts/write-test-registry.mjs:936-943,1109-1116` and generated `tests/registry.json:2956-2957,3535-3536` name the reachability, tab-order, and legibility outputs; `e2e/README.md` names `e2e/helpers/`; the final tree is self-consistent with 16/16 (8 cases × 2 projects), no strays, nothing staged |
| RV-1 (P2) | CORRECTED | `content/sittings/widen-2-lights.json:11` "This is the street object for lights."; `tests/widen-sitting-2.test.ts:23-39` literal plus negatives for every displayName and for `crossing`/`crosswalk`/`gates`/`signal`/`shop`; `e2e/specs/widen-2-lights.spec.ts:56-62` literal after two shop taps plus `toHaveCount(0)` on the old form; `docs/settled.md:21` D1 true for both sittings | None |
| COPY-1 (P2) | CORRECTED | `src/app/renderers/BusyBlock.tsx:868,1040` far pole and truck take `objectShapeClass(...)` → `.quiet-shape` (`:498-501` fallback; `src/index.css:89-95` filter plus dasharray); `tests/hotspot-layout.test.ts:70-78` asserts the two exact class strings, exactly 12 `objectShapeClass('obj-` call sites, exactly 14 bare `<g>` lines (counted against the live file); scratch mutant reverting the truck group fails three of four assertions; `docs/open-faults.md:21` F-unlisted-objects names pole, truck, and sitting 1's conductor and transformer | None for the styling defect. Missing hotspots stay open as F-unlisted-objects by owner pick; this verdict does not close that row |
| GI-1 (P2) | CORRECTED | `tools/check-gate-integrity.mjs` `forbid-only-missing` detector with self-test plants for `forbidOnly` absent and `forbidOnly: false`; `tests/gate-integrity.test.ts:69-80` same plants with literal `toEqual(['forbid-only-missing'])`; control count computed from `SELF_TEST_CONTROLS.length`; scratch kill proof: detector deleted → exit 1 and the unit case fails | None |
| GI-2 (P2) | CORRECTED | `tools/check-gate-integrity.mjs:62-69` six `REQUIRED_SCAN_TARGETS`, `population-missing` / `population-extra` seeded before any file is read; CLI prints `0 problems over 37 files`; `tests/gate-integrity.test.ts:155-189` independent `git ls-files -co --exclude-standard` list asserted `toEqual`, four negatives, live scan; `.claude/skills/drift-check/SKILL.md:63` gate-integrity report row; narrowed-filter probe → 11 `population-missing` | None |
| GI-3 (P2) | CORRECTED | False "Vitest has no retry" removed (`docs/testing-gauntlet.md:63,135`); retry detectors for `retry:`/`retries:` keys, `vi.setConfig`, `describe.configure`, `--retry`/`--retries` flags; `tools/check-gate-integrity.mjs:95` regex accepts bare or quoted keys; control `quoted-retry-keys` at `:373-397` (15 controls total, `--self-test` 15/15); `tests/gate-integrity.test.ts:129-143` literal code arrays for quoted forms plus clean `'retry': 0`; `docs/testing-gauntlet.md:218` G-unit row lists all 13 codes, `:243` Honest limits names quoted keys as seen | None for the finding. Non-blocking: computed property keys (`{ ["retry"]: 2 }`) evade the regex and are not named in Honest limits; implementer's discretion, owner informed |
| SCH-1 (P2) | CORRECTED | `tests/widen-sitting-1.test.ts:80-186` and `tests/widen-sitting-2.test.ts:99-208` eight negatives each (approachLiveGear const, required secondMissHint, prompt minLength, minItems, gloss minLength, through-line required gloss, hotspot additionalProperties) with literal Ajv paths; in-memory schema mutations flip 16/16; `node tools/blast-radius.mjs --word approachLiveGear` lists both sitting tests | None |
| SCH-2 (P2) | CORRECTED | `tools/check-schemas.mjs:115-253` seven controls, every planted one through `findProblems` into a `mkdtempSync` root; `instance-invalid` mutates the real title instance; unused `badTitle` gone; `tests/schema-inventory.test.ts:125-130` literal `7/7 controls`; loader-hook mutants each give 6/7 exit 1 | None blocking; no tool enforces a negative Ajv test per new schema (standing position) |
| ETHOS-1 (P2) | CORRECTED | `scripts/lib/client-gates.mjs:16-59` 24 substring plus 4 bounded tokens covering every token named; `tests/client-gates.test.ts` literal 28-entry list, one control per token, boundary negatives, live `src/` scan; `tools/pii-lint.mjs` 8/8 and `ethos: 0 problems over 28 tokens`; `docs/testing-gauntlet.md:217` names groups and the aliasing blind spot | None blocking; `scanEthos` covers `src/` only and `index.html` loads only `/src/main.tsx` |
| PW-2 (P2) | CORRECTED | `src/index.css:157-162` `.hotspot:focus-visible { outline: 4px solid #b8770a; outline-offset: 3px }` beats every state class; `e2e/helpers/hotspot-reach.ts:63-71` asserts literal `rgb(184, 119, 10)` / `4px` / `focusVisible: true` after Tab in both sitting specs; the prior measurement `rgb(18, 32, 42) solid 3px` would fail `toEqual` | None blocking |
| PW-3 (P2) | CORRECTED | `e2e/helpers/hotspot-reach.ts:40-74` tabindex counts equal through-line and off-need counts, Shift+Tab walk equals the reversed literal list, one further Shift+Tab lands on no hotspot, forward order; literal lists in both specs; JSON-layer assertions retained | None; registry expected_outputs now name tab order and focus ring (closed in second pass) |
| PW-4 (P2) | CORRECTED | transformer widthPercent 10.4 → 74.9 px at the 720 px floor; `e2e/helpers/hotspot-reach.ts:77-87` `expectNamesReadable` asserts `.hotspot-name` count then `clipped toEqual([])`, run with Show all names on (8 and 10 names); replica oracle flips at a 72 px box and at the old 6.2 % box; `toHaveText('Distribution transformer')` retained | None reopening; vertical half of the oracle is vacuous by construction, transformer label has 2.0 px of slack at 720 px |
| PW-5 (P2) | CORRECTED | Hotspots: reachability helper on all 18 ids, geometric detector with literal negatives at `tests/hotspot-layout.test.ts:47-74`; controls: `e2e/helpers/hotspot-reach.ts:87-96` `expectControlsAtLeast44` (visible, `boundingBox()` not null, width and height ≥ literal 44) on `Show all names` / `Back to title` at `widen-1-get-across.spec.ts:100`, `widen-2-lights.spec.ts:124` and on `Get across` / `Lights` at `title.spec.ts:17`; `tests/hit-target.test.ts:10-18` exact six-line block match on `src/index.css:27-32` (byte-for-byte, LF-only) replacing the substring check | None for the finding. Non-blocking: the adult reveal button is covered by the CSS rule but not by a measured `boundingBox` |
| DRIFT-01 (P2) | CORRECTED | `CLAUDE.md:8`, `README.md:12,16-17`, `SPEC.md:22-23` say playable with the copy gate open; `SPEC.md:29` "Status words" defines the feature map's `shipped` as the lockstep label only; `docs/settled.md:21` D2 and `docs/open-faults.md:10` F-lessons agree; `node tools/check-lockstep.mjs` 6 parts, 0 problems | None blocking; F-prose is the declared oracle for the sentence |
| DRIFT-02 (P2) | CORRECTED | `docs/level-spine.md:4,6,159-161` "Build Learn levels only with this file as the brief", "Widen sittings 1–2 are built from it; sittings 3–11 and every deepen rung are unbuilt"; agrees with `README.md:49`, `docs/open-faults.md:13`, `SPEC.md:23` | None |
| P3 rows (COPY-2, GI-4–GI-7, GI-9, SCH-4, SCH-5, PII-1, PII-2, PW-7, PW-8, PW-9, DRIFT-03, DRIFT-05, DRIFT-06, DRIFT-08, DRIFT-09, DRIFT-10) | PARTIAL | `docs/open-faults.md:20-29` rows F-svg-description, F-unlisted-objects, F-gip-limits, F-sitting-schema-pins, F-pii-scan-quiet-skip, F-git-identity, F-e2e-proof-gaps, F-sitting-session-key, F-doc-duplication, F-gate-load-sensitivity, each with Where and Done means, no personal names (`node tools/pii-lint.mjs` 0 problems); DRIFT-05 hint half and DRIFT-10 reveal precondition and FEAT-TITLE parity fixed in content/SPEC/README; F-unlisted-objects scopes all four un-hotspotted drawn objects; F-doc-duplication names the F-gip-github clause; F-gip-limits omits the refuted GI-8; `docs/settled.md:23` Q-review-triage-2026-08-21 records the owner picks and each claim checks out | Three accuracy nits inside the rows, each a one-line edit, non-blocking: (a) F-gate-load-sensitivity (`docs/open-faults.md:29`) restates measurements owned by Q-check-stall-2026-08-21 (`docs/settled.md:20`) and the two rows give different timings for the same pii-scan git calls (0.09 s + 0.51 s versus 0.15 s + 0.23 s) with neither labelling its host condition; (b) F-gip-limits (`:22`) opens "Named in the gauntlet's Honest limits" but `docs/testing-gauntlet.md:240-248` does not name the GI-6 single-plant or GI-7 word-boundary limits; (c) F-doc-duplication's Where column (`:28`) omits `docs/settled.md:22`, `tools/file-map.mjs`, and `docs/open-faults.md:17` where the DRIFT-08 restatements also live |

### Second pass

The first pass left four items PARTIAL. COPY-1 closed: the quiet class on the far pole and truck is now pinned by four literal assertions in `tests/hotspot-layout.test.ts:70-78`, a scratch mutant reverting one group fails three of them, and the missing hotspots are carried by F-unlisted-objects as the owner's pick. GI-3 closed: the retry regex at `tools/check-gate-integrity.mjs:95` accepts quoted keys, the `quoted-retry-keys` control brings the self-test to 15/15, the unit test pins the quoted forms, and the gauntlet names the behaviour. PW-5 closed: `expectControlsAtLeast44` measures the rendered `Show all names`, `Back to title`, `Get across`, and `Lights` buttons with the literal 44 in both axes, and the stylesheet unit test now matches the exact six-line rule block instead of a substring. The PW-1 notes closed: inspiration wording, registry expected_outputs, `e2e/README.md`, and the proof-ordering caveat are all consistent with the final tree. P3-ROWS closed on every named residue (FEAT-TITLE parity, the sitting-1 conductor and transformer, the F-gip-github duplication clause, the GI-8 label) and stays PARTIAL only for the three accuracy nits in the table above, which sit inside open-faults rows, are the implementer's discretion under the review's P3 rule, and do not block integrate.

### Regressions observed

None. Across both passes: no test deleted, skipped, or marked `only`/`fixme`/`todo` (gate-integrity 0 problems over 37 files; grep of `tests/` and `e2e/` clean); every floor in `.claude/gate-baseline.json` rose or held (vitest_files 12 → 13, vitest_cases 51 → 92, playwright_specs 4, schema_files 7, shipped_features 2) and the `history_files_max` ceiling is untouched; `playwright.config.ts:16` keeps `retries: 0` with `forbidOnly: true`; every new assertion uses a literal expected value and none reads the constant it checks (E2); no oracle was replaced by a weaker one (`toHaveText`, JSON-layer `inTabOrder`, the prior Ajv negatives, and the original hit-target check's intent all remain beside the new proofs). The 720 px block with in-frame panning is a product fix recorded in `SPEC.md:23` and `CHANGELOG.md:33`, not testing work, so E8 is not engaged. One pre-existing aside outside any finding: `scripts/write-test-registry.mjs:949,1122` list "Playwright trace on retry" as a failure diagnostic while `retries: 0` with `trace: 'on-first-retry'` never produces one; F-e2e-proof-gaps (`docs/open-faults.md:26`) already carries that fault.

### Gates on the final tree

| check | result |
|---|---|
| Vitest | 13 files / 92 cases (re-run by ROLE-REVIEWER per file where cited; totals from ROLE-IMPLEMENTER's gauntlet run) |
| npm run check | exit 0 |
| npm run gauntlet | exit 0; tsc; vite build; Playwright 16 passed across chromium-desktop and chromium-mobile (ROLE-IMPLEMENTER's run in a quiet host window; not re-run in this verification) |
| Floors | vitest_files 13, vitest_cases 92 (raised from 12 / 51); no floor lowered, no ceiling raised |
| Gate integrity (G-unit) | 0 problems over 37 files · 15/15 controls |
| Schema inventory (G-schema) | 7 schemas, 7 mapped, 0 problems · 7/7 controls |
| PII lint | 8/8 controls |
| Ethos (G-ethos) | 0 problems over 28 tokens |

## Disposition

- Integrate is clear. PW-1 (P1) and all fourteen P2 findings (RV-1, COPY-1, GI-1, GI-2, GI-3, SCH-1, SCH-2, ETHOS-1, PW-2, PW-3, PW-4, PW-5, DRIFT-01, DRIFT-02) are CORRECTED and verified by the same reviewer. No finding is NOT_CORRECTED.
- The owner's triage picks (Q1 A, Q2 A, Q3 A) were applied and are recorded as `docs/settled.md:23` Q-review-triage-2026-08-21; the corrections verified above are the form those picks took on disk.
- P3 findings live as rows in `docs/open-faults.md:20-29`. The one remaining PARTIAL (P3 rows) is three accuracy nits inside those rows, named in the table; they are the implementer's discretion with the owner informed and do not block integrate. Non-blocking observations recorded beside GI-3 (computed property keys) and PW-5 (adult reveal button not measured) are the same class.
- D1, D3, D4, and D5 are verified on disk; RV-1's reopening of D1 for sitting 2 is closed.
- D2 (the human ROLE-EDITOR copy gate, `F-lessons`) remains open regardless of this record. This record does not close it, and nothing here certifies kid-facing copy as passed.
- This verification was read-only. Unit tests and tools were re-run where cited; Playwright results rest on ROLE-IMPLEMENTER's gauntlet run of the final tree, whose case count, baseline, and file set are consistent with the tree inspected.
- `F-sittings-reverify` and `F-gip-review` in `docs/open-faults.md` are satisfied by this section being filled by ROLE-REVIEWER; the header Disposition field of this record should be updated to match this section.

Implementer note (ROLE-IMPLEMENTER, after the second pass): the three accuracy nits named in the P3-rows line were corrected in the same commit — F-gate-load-sensitivity now points at Q-check-stall-2026-08-21 for its measurements, F-gip-limits no longer claims the gauntlet names every listed evasion, and F-doc-duplication's Where column lists every file that restates the gate count.
