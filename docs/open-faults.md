# Open faults

**This document owns** what is known wrong, missing, or undecided right now, with where it lives and what done means.
**It does not own** anything closed.

A fault that lives only in a chat log is a fault this project will lose.

| ID | Where | What's wrong | Done means |
|---|---|---|---|
| F-lessons | `src/`, `content/` | Sitting 1–2 copy exists; ROLE-EDITOR human gate still required | First Learn cards exist and pass human copy gate |
| F-profiles | `src/` | On-device sibling profiles are schema-only | A local profile picker writes gitignored saves |
| F-cosmetics | `cosmetics/` | Album/hat **art** is unbuilt; **rules** are frozen in `docs/level-spine.md` | Stub assets exist; no collectible UI until Learn follows the spine |
| F-spine-lessons | `docs/level-spine.md` | Widen #1–2 are playable; sittings 3–11 and deepen remain unbuilt | First Learn sittings match widen order 1–3 without inventing a second curriculum |
| F-runner | `/workflows` | Declarative graphs only; no executable runner | Opt-in runner later, never inside the child PWA |
| F-prose | all OWNER docs | A stale paragraph in fresh words is invisible to the maps | Human read of governing docs at release |
| F-exclusion-review | `tests/registry.json` | Taxonomy exclusions still wait on ROLE-REVIEWER verification of this slice | Independent review record marks them verified |
| F-gip-github | GitHub | GIP-C1 CODEOWNERS and GIP-C2 branch protection are absent; this repository has no `.github/` workflows | Operator enables CODEOWNERS and required checks when pull requests exist; do not fake them in markdown |
| F-sittings-reverify | `docs/reviews/` | Sittings 1–2 fixes D1–D5 are on disk but the same ROLE-REVIEWER has not re-verified them; no sittings review record exists | Same-reviewer record under `docs/reviews/` marks D1–D5 corrected; integrate stays blocked until then or an owner exception |
| F-gip-review | `tools/check-gate-integrity.mjs`, `docs/testing-gauntlet.md` | The paper-over scanner and the expanded Agent contract have had no independent review | ROLE-REVIEWER record under `docs/reviews/` |
| F-unlisted-objects | `src/app/renderers/BusyBlock.tsx`, `content/sittings/` | The far utility pole and the street truck are drawn quiet in both sittings but have no hotspot, and sitting 1 also draws the overhead conductor and distribution transformer without hotspots, so a tap on any of them is a silent no-op and Show all names is not symmetric across the block (review COPY-1 residue, DRIFT-05) | Every drawn object is either a hotspot with a real name or is absent from the block |
| F-gip-limits | `tools/check-gate-integrity.mjs` | Uncovered evasions: `it.fails` / `test.fixme` / `it.todo` / `it.skipIf` are not detected; config checks are comment-blind substring regexes; only one alternative of each multi-pattern detector has a control; skip/only patterns lack a leading word boundary; `\|\| echo ok`, `; exit 0`, and `--passWithNoTests` are not detected (review GI-4, GI-5, GI-6, GI-7, GI-9) | Each named evasion has a detector with a negative control, or an owner-accepted exception recorded in `docs/settled.md` |
| F-sitting-schema-pins | `schema/sitting-widen-1.schema.json`, `schema/sitting-widen-2.schema.json` | Sitting 1 does not pin `obj-crossing-gates` as through-line; sitting 2 pins only the traffic signal and shop as off-need, so a crossing object moved onto the Lights through-line still validates; hotspot ids are not required to be unique (review SCH-4, SCH-5) | Each sitting schema pins every curriculum role and rejects duplicate ids, with a negative Ajv test per pin |
| F-pii-scan-quiet-skip | `scripts/lib/pii-scan.mjs` | A candidate file that cannot be read, or whose git path is C-quoted (non-ASCII), is skipped with no signal (review PII-1) | Unreadable or undecodable candidates fail the scan loudly |
| F-git-identity | `docs/PRIVACY.md`, operator git config | Commits carry a GitHub noreply address under a handle other than the `opencutaway` identity the privacy document prescribes (review PII-2); agents may not run `git config` | The operator sets the documented identity for future commits; any history rewrite is the owner's call |
| F-e2e-proof-gaps | `e2e/specs/`, `playwright.config.ts` | Reduce-motion is proven only by a CSS string check, not by a Playwright run with `reducedMotion: 'reduce'`; `trace: 'on-first-retry'` with `retries: 0` means a failing run never records a trace (review PW-7, PW-8) | A reduce-motion Playwright step asserts no animation on the through-line hotspot; failing runs retain a trace |
| F-sitting-session-key | `src/app/App.tsx`, `src/app/WidenSitting1.tsx` | Session state is not keyed per sitting, so a direct hash change from `#/get-across` to `#/lights` keeps sitting 1's `foundId` and can show the Traffic signal card inside Lights; no spec covers hash navigation (review PW-9) | Switching sitting resets the session, proven by a Playwright hash-navigation step |
| F-doc-duplication | `CHANGELOG.md`, `docs/testing-gauntlet.md`, `.cursor/rules/drift-maps.mdc`, `docs/settled.md`, `tools/file-map.mjs`, this file | The 0.0.0 changelog entry contradicts itself and lists version headings for states that never became commits; the decision-page format and the silence-is-not-approval rule are restated in the gauntlet though `CLAUDE.md` owns them; the gate count is restated in wordings the copied-fact detector cannot see; the gauntlet cites section numbers of a draft policy that is not in the repository; the `F-gip-github` row above restates the GIP-C1/C2 status cells of the gauntlet (review DRIFT-03, DRIFT-06, DRIFT-08, DRIFT-09) | One owner per fact, the changelog matches the commits, and no citation points outside the repository |
| F-gate-load-sensitivity | `tests/pii-scan.test.ts`, `playwright.config.ts`, operator environment | When other sessions saturate this host (measurements live in `docs/settled.md` Q-check-stall-2026-08-21), gates breach walls that measure the environment rather than the code: the pii-scan test's cold read of its candidate files missed the 5 s wall and Playwright's `vite preview` web server missed its 60 s start-up limit, each once; every such run passed when rerun on an idle host, and the 5 s wall stays where it is | Gates measure their own work, not first-open or process-spawn latency: the operator runs gates while other sessions are idle (or excludes the checkout from real-time scanning, an operator decision outside this repository), and any gate change keeps the 5 s per-test wall |
| F-art-geometry-migration | `src/app/renderers/BusyBlock.tsx`, `content/sittings/*` | The placeholder block is drawn on a 960×540 viewBox with 72×36 tiles and a 720 CSS px minimum width, while real art is specified in `docs/art-bible.md` and `content/art/tokens.json` at 1024×768 / 64×32 / 16 px; the exemption is named here, not silent | The block is redrawn on the real-art grid and the hotspot layout rule in `SPEC.md` is re-expressed against that grid |
| F-deepen-rungs-unbuilt | `SPEC.md` deepen-rung section, `src/` | The rung 2–3 interaction model is designed and nothing is built: no hub, no inspection scene, no process animation, no narration, no audio, and no audio licence rows exist | A first carrier-rung lesson passes E12 (all six drift parts, schema plus Ajv, Playwright teaching, interaction, and regression steps) with bundled audio that clears licence review and `CLAUDE.md` S4 review |
