# Settled questions

**This document owns** questions a person or a measurement has already closed. It only grows.
**It does not own** what is still open.

Never reopen a closed question without the owner.

| ID | Question | Closed as | Evidence |
|---|---|---|---|
| Q-name | Display name | Open Cutaway | Locked product decision |
| Q-repo | Public GitHub | `opencutaway/opencutaway` | Locked product decision |
| Q-stack | Client stack | TypeScript + Vite + Preact; no Next.js; no backend | Locked product decision |
| Q-domain | Subject matter | Infrastructure literacy, not phonics | Locked product decision |
| Q-backbone | Engineering pattern source | Maps, owners, blast radius, floors, document split — process only | Locked product decision |
| Q-inspiration-scarry | Extra visual tone | Richard Scarry, *What Do People Do All Day?*, as occupational-density reference only; do not copy characters, names, town, art, or text | Owner request 2026-08-18 |
| Q-six-drift-parts | Drift-prevention parts | Six named parts in lockstep: Owners, Code map, Feature map, Tutorial manifest, Blast radius, Gates | Owner request 2026-08-18 |
| Q-ajv-playwright-growth | Player-facing proof | JSON Schema plus Ajv is the authority path; Playwright specs grow with each player-facing feature | Owner request 2026-08-18 |
| Q-level-spine | How levels are numbered and taught | `docs/level-spine.md`: busy-block widen then deepen; rungs default to three (not a law); combined-sewer hydro town; verbs climb with distance | Owner questionnaire 2026-08-18–19 and civil-expert interview 2026-08-19 — read `docs/level-spine.md`, do not copy the freeze here |
| Q-art-runtime | Block art and client cost | Runtime stands: 2D, no WebGL, no huge textures, modest machines and iPads; the look itself is now owned by `docs/art-bible.md` (see Q-art-bible-2026-08-21), which replaces the named-style phrasing with rules | Owner request 2026-08-19 — freeze text in `SPEC.md` (shipped look) and `docs/inspiration.md` (style + do-not-copy); `docs/level-spine.md` points |
| Q-check-stall-2026-08-21 | Was the 2026-08-21 `npm run check` stall (silent ~196–202 s before the vitest banner) a product fault? | No. The stall is memory pressure on the host, not a test: with 15.8 GB of RAM, 1.6 GB free, commit at 91%, 2,663 pages/s, and a disk queue of 11 (other agents' sessions, browsers, and an antivirus engine resident), vitest sat 96.7 s before its banner and a cold read of the 121 PII-scan candidate files took 12.98 s; on the same day with the host idle the banner appeared after 1.9 s, the same read took 0.71 s, every run passed (13 files, 92 cases), and the full `check` chain went green in 8 s. The pii-scan helper's two git calls take 0.15 s + 0.23 s and throw on a 2 s timeout rather than hang. Mitigation is outside the repository (free host memory or run gates while other sessions are idle; the 5 s per-test wall is untouched because the delay is paging, not test work). Reopen only if a stall appears on an idle host | Measurement 2026-08-21 |
| Q-sittings-review-2026-08-21 | Sittings 1–2 review decisions D1–D6 | D1 second-miss hint names the need, never the object; D2 the human ROLE-EDITOR copy gate stays ahead of "shipped" (`F-lessons`); D3 transformer gloss leads with `distribution transformer`; D4 the title spec clicks Lights; D5 adult reveal and Show all names run on both sitting specs through one shared helper; D6 keep `5d0d24d` unamended, sittings land as the next commit | Owner picks 2026-08-20–21; strings and specs on disk |
| Q-gip-adopt | How gate-integrity controls enter the project | Strengthen in place under the existing 11 gates: the paper-over scanner is G-unit, not a 12th gate; controls are named GIP-C1–C9, never E1–E9; no `GIP.md` as a tenth governing file; E12 carve-out — adding schema, Ajv, Playwright, and raising floors is required, weakening any oracle needs an owner decision page | Owner picks 2026-08-21; text lives in `docs/testing-gauntlet.md` |
| Q-review-triage-2026-08-21 | Scope of corrections from the CTX-REVIEW-SITTINGS-GIP review | Fix the P1 and all fourteen P2 findings before the sittings commit; P3 findings become rows in `docs/open-faults.md`; sitting 2's second-miss hint is `This is the street object for lights.`; prose says *playable, copy gate open* and `SPEC.md` defines the feature map's `shipped` label as the lockstep label | Owner picks 2026-08-21; record `docs/reviews/CTX-REVIEW-SITTINGS-GIP.md` |
| Q-art-bible-2026-08-21 | How the Visual Design Bible v1.2 enters the project | `docs/art-bible.md` is governing document #10 and owns the look; real art uses the 1024×768 logical canvas, 64×32 ground diamond, and 16 px elevation unit on every supported device, with narrower screens fitting to width and panning the block inside its own frame; occupational density stays as settled (Q-inspiration-scarry, `docs/level-spine.md`) and the bible's ambient budget governs animation only — many drawn, few moving; iPad landscape is primary, phones and desktops supported, ages 7–12; the interaction model lives in `SPEC.md` as the deepen-rung design, not in the bible; look values live in `content/art/tokens.json` under Ajv with a gate that holds renderer constants and stylesheet colours to them; the shipped SVG block is a named placeholder exemption (`F-art-geometry-migration`) | Owner picks 2026-08-21 from the Visual Design Bible v1.2 adoption page |
| Q-art-subjects-2026-08-21 | Do the art bible's ten lesson subjects map onto the spine's eleven needs or replace them? | Map. The eleven needs and their rungs stay as `docs/level-spine.md` freezes them; each bible subject is a named object or scene on one need at one rung (signal and crossing, railway crossing, and bridge on Get across; inlet and outfall on Rain; hydrant and main on Drink; sanitary sewer on Flush; intermodal on Goods; the hydroelectric facility on Lights; the waste campus on Trash). Needs with no bible subject take their blueprints from the spine directly | Owner pick 2026-08-21; table in `docs/art-bible.md` |
| Q-copy-gate-2026-08-21 | Sittings 1–2 kid-facing copy after the human read-through | 40 of 43 strings pass as written; the need shown as "Get across" is named **Cross the Street** on screen (title button, title blurb, sitting 1 heading) and the title blurb reads "Progress always stays on this device."; internal slugs keep `get-across`; the prompt and second-miss hint keep lower-case "get across" | ROLE-EDITOR copy gate 2026-08-21, record `docs/reviews/CTX-EDITOR-SITTINGS-1-2.md` |
